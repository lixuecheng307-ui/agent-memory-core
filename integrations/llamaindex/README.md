# LlamaIndex Adapter

Status: planned.

Recommended shape:

- use Agent Memory Core as an external memory backend
- keep API keys and model providers owned by the host application
- use `retrieve_with_trace()` for auditable retrieval
