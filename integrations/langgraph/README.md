# LangGraph Adapter

Status: planned.

Recommended shape:

- a node that calls `build_context_with_trace()`
- a node that calls `remember()` only after host policy approval
- trace metadata attached to graph state
