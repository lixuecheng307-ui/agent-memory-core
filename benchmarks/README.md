# Benchmarks

Run:

```powershell
memory-core benchmark
```

Outputs:

- console table
- `benchmark_results.json`
- `benchmark_report.md`

Metrics include Recall@K, project leakage, disabled/deleted recall, sensitive
injection, conflict resolution accuracy, average retrieval latency, and context
token estimate.
