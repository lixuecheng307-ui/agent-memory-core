# Claude Code Notes

Agent Memory Core can be used from Claude Code through the generic Python API or
the optional MCP stub.

Keep project-specific policy outside `memory_core/`. Pass the project ID and any
API keys explicitly from the host process.
